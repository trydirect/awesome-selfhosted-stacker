# Copy this file to .env and fill in before first deploy:
#   cp .env.example .env && ./scripts/generate-secrets.sh

# The bundled config.yaml is the source of truth for the default local setup.

# No additional environment variables are required for the default local setup.
