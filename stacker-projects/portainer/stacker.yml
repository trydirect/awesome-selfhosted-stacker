name: portainer

app:
  type: custom
  image: portainer/portainer-ce:latest
  ports:
    - "9000:9000"
  volumes:
    - portainer_data:/data
    - /var/run/docker.sock:/var/run/docker.sock

volumes:
  portainer_data: {}

proxy:
  type: none
  auto_detect: false

env_file: .env
