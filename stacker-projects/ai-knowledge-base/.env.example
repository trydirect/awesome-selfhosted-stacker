# AI Knowledge Base — Dify + Qdrant + Ollama RAG Stack
# Copy to .env and generate secrets before first deploy:
#   cp .env.example .env && ./scripts/generate-secrets.sh

# Secrets (generated by scripts/generate-secrets.sh)
SECRET_KEY=
DB_PASSWORD=
POSTGRES_PASSWORD=
QDRANT_API_KEY=
INIT_PASSWORD=
APP_SECRET_KEY=

# Deploy target (server)
DEPLOY_HOST=
BASE_PATH=${BASE_PATH}
