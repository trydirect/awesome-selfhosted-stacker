name: automatisch

project:
  identity: automatisch

install:
  inputs:
    commonDomain: automatisch.try.direct

app:
  type: custom
  image: trydirect/automatisch:latest
  ports:
    - "3000:3000"
  environment:
    APP_SECRET_KEY: ${APP_SECRET_KEY}
    ENCRYPTION_KEY: ${ENCRYPTION_KEY}
    WEBHOOK_SECRET_KEY: ${WEBHOOK_SECRET_KEY}
    REDIS_HOST: redis
    POSTGRES_HOST: automatisch-db
    POSTGRES_PORT: "5432"
    POSTGRES_DATABASE: automatisch
    POSTGRES_USERNAME: automatisch
    POSTGRES_PASSWORD: ${DB_PASSWORD}
    WEB_APP_URL: http://localhost:3000

services:
  - name: automatisch-db
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: automatisch
      POSTGRES_USER: automatisch
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - automatisch_db:/var/lib/postgresql/data
    healthcheck:
      test: CMD-SHELL pg_isready -d automatisch -U automatisch
      interval: 10s
      timeout: 5s
      retries: 10

  - name: redis
    image: redis:7-alpine
    healthcheck:
      test: CMD redis-cli ping
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  automatisch_db: {}

proxy:
  type: caddy
  auto_detect: false
  domains:
    - domain: automatisch.example.com
      ssl: off
      upstream: app:3000

hooks:
  pre_build: ./scripts/generate-secrets.sh

monitoring:
  status_panel: true

env_file: .env

deploy:
  target: server
  server:
    host: ${EXISTING_SERVER_HOST}
    user: ${EXISTING_SERVER_USER}
    ssh_key: ${BASE_PATH}/stacker-project-test
  cloud:
    provider: hetzner
    region: fsn1
    size: cpx22
    ssh_key: ${BASE_PATH}/stacker-project-test
    public_ports:
      - "80"
      - "443"
      - "3000"
